server {
    server_name ~^(?<subdomain>.+)<%= "\.#{@regex_domain}" %>$;

    listen 443 ssl;
    set $backend $subdomain.<%= @int_domain %>;

    location / {
        include /etc/nginx/proxy;
    }
}
